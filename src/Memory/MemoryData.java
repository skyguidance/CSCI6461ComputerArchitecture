package Memory;

/**
 * Here is the memory data class.
 * Each object will hold a memory info.
 */
public class MemoryData {
    int address;
    int value;

    public MemoryData(int address, int value) {
        this.address = address;
        this.value = value;
    }
}
